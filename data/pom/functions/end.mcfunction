# hide the bossbar
bossbar set pom:time visible false

# stop the clock
scoreboard players set running pom.time 0

# shows the options menu in case the player wants to adjust something
function pom:options
